{
  "name": "websocket-server",
  "version": "1.0.0",
  "description": "WebSocket server with CORS and rate limiting",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "express-rate-limit": "^5.3.0",
    "sanitizer": "^2.0.1",
    "ws": "^8.7.0"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
